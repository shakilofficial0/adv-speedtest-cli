#!/usr/bin/make -f

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_install:
	python3 setup.py install --root=debian/adv-speedtest-cli --install-layout=deb

override_dh_python3:
	dh_python3 --shebang=/usr/bin/python3
